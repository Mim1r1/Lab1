# GPS Monolith (generated)

This repository contains a generated Rails 7 monolith application named `gps_monolith` (core files added).

Quickstart:
1. git checkout feature/gps-monolith
2. bundle install
3. cp config/database.yml.example config/database.yml and configure DB credentials
4. bin/rails db:create db:migrate db:seed
5. bin/rails server
